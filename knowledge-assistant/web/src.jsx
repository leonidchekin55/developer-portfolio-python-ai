import React,{useEffect,useRef,useState} from 'react';import{createRoot}from'react-dom/client';import{Upload,BookOpen,Send,FileText,LoaderCircle, Sparkles,Clock3}from'lucide-react';import'./style.css';
const API='/api/v1';function App(){const[docs,setDocs]=useState([]),[history,setHistory]=useState([]),[q,setQ]=useState(''),[answer,setAnswer]=useState(null),[mode,setMode]=useState('ollama'),[uploadLimit,setUploadLimit]=useState(20),[busy,setBusy]=useState(false),[uploading,setUploading]=useState(false),[error,setError]=useState(''),input=useRef();async function refresh(){try{const[d,h,m]=await Promise.all([fetch(API+'/documents').then(r=>r.json()),fetch(API+'/history').then(r=>r.json()),fetch(API+'/mode').then(r=>r.json())]);setDocs(d);setHistory(h);setMode(m.rag_mode);setUploadLimit(m.max_upload_mb||20)}catch{}}useEffect(()=>{refresh()},[]);async function upload(file){if(!file)return;setUploading(true);setError('');const fd=new FormData();fd.append('file',file);try{const r=await fetch(API+'/documents',{method:'POST',body:fd}),data=await r.json();if(!r.ok)throw Error(data.detail||'Не удалось загрузить файл');await refresh()}catch(e){setError(e.message)}finally{setUploading(false);input.current.value=''}}async function ask(e){e.preventDefault();if(!q.trim())return;setBusy(true);setError('');try{const r=await fetch(API+'/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:q})}),data=await r.json();if(!r.ok)throw Error(data.detail||'Ошибка запроса');setAnswer({question:q,...data});setQ('');await refresh()}catch(e){setError(e.message)}finally{setBusy(false)}}return <main><header><div className="brand"><span className="logo"><Sparkles size={20}/></span><div><b>Knowbase</b><small>Поиск по вашим документам</small></div></div><span className="local">● {mode==='extractive'?'Бесплатное облачное демо':mode==='ollama'?'Локальный режим':'RAG на сервере'}</span></header><section className="hero"><div className="eyebrow">PERSONAL KNOWLEDGE, UNLOCKED</div><h1>Ваши документы.<br/><em>Осмысленные ответы.</em></h1><p>Загрузите материалы и задавайте вопросы. Каждый ответ будет основан на ваших источниках.</p></section><div className="grid"><section className="panel docs"><div className="panelhead"><div><span className="iconbox"><BookOpen size={18}/></span><h2>Библиотека</h2></div><span className="count">{docs.length} файлов</span></div><label className="drop" onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();upload(e.dataTransfer.files[0])}}><input ref={input} type="file" accept=".pdf,.docx,.txt" onChange={e=>upload(e.target.files[0])}/><Upload size={21}/><b>{uploading?'Индексируем…':'Добавить документ'}</b><small>PDF, DOCX или TXT · до {uploadLimit} МБ</small></label><div className="doclist">{docs.map(d=><div className="doc" key={d.id}><span className="file"><FileText size={17}/></span><div><b>{d.filename}</b><small>{d.chunks} фрагментов · {d.status==='indexed'?'Готово':d.status}</small></div><i className={d.status==='indexed'?'ok':'pending'}>●</i></div>)}{!docs.length&&<div className="empty">Здесь появятся загруженные документы</div>}</div><div className="privacy">Общее временное демо: документы и вопросы могут видеть другие посетители. Не загружайте личные или конфиденциальные файлы.</div></section><section className="panel chat"><div className="panelhead"><div><span className="iconbox violet"><Sparkles size={18}/></span><h2>Спросите документы</h2></div><span className="model">{mode==='extractive'?'ПОИСК ПО ТЕКСТУ · ИСТОЧНИКИ':mode==='ollama'?'OLLAMA · RAG':'AI-ГЕНЕРАЦИЯ · RAG'}</span></div><div className="conversation">{!answer?<div className="welcome"><div className="orb"><Sparkles size={26}/></div><h3>Что хотите узнать?</h3><p>Ответы будут ссылаться на конкретные фрагменты ваших документов.</p><div className="suggestions" onClick={e=>e.target.tagName==='BUTTON'&&setQ(e.target.textContent)}><button>Кратко перескажи главное</button><button>Какие ключевые даты указаны?</button><button>Найди основные выводы</button></div></div>:<article className="answer"><div className="question">{answer.question}</div><div className="answertext">{answer.answer}</div><h4>ИСТОЧНИКИ</h4>{answer.sources.map((s,i)=><div className="source" key={i}><FileText size={15}/><span><b>{s.filename}</b><small>Страница {s.page} · {s.text}</small></span></div>)}</article>}</div><form className="ask" onSubmit={ask}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Задайте вопрос по документам…"/><button disabled={busy||!q.trim()}>{busy?<LoaderCircle className="spin" size={19}/>:<Send size={18}/>}</button></form><div className="footnote">AI может ошибаться. Сверяйте ответы с оригиналом.</div></section></div>{error&&<div className="error">{error}</div>}<section className="recent"><div className="recenthead"><Clock3 size={17}/> Недавние вопросы</div>{history.slice(0,3).map(m=><button className="recentitem" key={m.id} onClick={()=>setAnswer({question:m.question,answer:m.answer,sources:m.sources})}>{m.question}<span>›</span></button>)}{!history.length&&<small>После первого запроса здесь появится история.</small>}</section><footer>KNOWBASE <span>·</span> Демоверсия · состояние может сбрасываться после простоя</footer></main>}createRoot(document.getElementById('root')).render(<App/>);
